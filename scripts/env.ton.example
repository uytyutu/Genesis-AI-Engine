# Virtus VCORE Genesis — TON TESTNET ONLY
# Copy to repo root as .env.ton (gitignored). NEVER commit real mnemonics.
#
# Faucet: https://t.me/testgiver_ton_bot
#
# Commands:
#   npm run vcore:genesis:init
#   npm run vcore:genesis:deploy
#   npm run vcore:genesis:transfer
#   npm run vcore:genesis:verify

TON_NETWORK=testnet
TON_MNEMONIC=word1 word2 ... word24

# Optional (toncenter rate limits):
# TONCENTER_API_KEY=

VCORE_SUPPLY=1000000
