# Скопируйте в dashboard/backend/.env.local и вставьте ключ.
# Genesis.exe подхватывает .env.local при старте backend.

# Автопилот Engine — поиск компаний в Германии
# Google Cloud Console → Places API → Credentials → API Key
GOOGLE_PLACES_API_KEY=
