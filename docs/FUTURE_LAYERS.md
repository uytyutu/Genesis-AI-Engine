# Future layers — not implemented

These ideas are **architecturally reserved** but **out of scope** until Factory and Command Center exist.

**Company vision (departments, autonomy levels, “why?”):** `docs/DIGITAL_COMPANY_VISION.md`

## Evolution (self-improvement via AuditStorage)

`AuditStorage` records outcomes — successes and failures.  
A future **Evolution Engine** (Phase 9) will read `audit.jsonl`, find patterns («high price → no sales»), and propose template changes — always with **explainable reason** for the owner.  
**Step 2 only writes events.** No analysis in v0.1.

## Crypto payment gateway

Autonomous payouts via blockchain — future **Billing Agent** + external API.  
**🔴 Level 3 — owner only.** Not in Brain v0.1. Requires owner approval, legal review, and security audit.

## Code and license protection

Product-level licensing (Telegram ID, domain binding) — future **Factory** concern.  
Genesis core (Brain, Kernel) stays private; no external API to control the studio without owner auth.

---

*First Factory vertical when coded: **Landing Page** (sandbox). Telegram and others follow after one template is proven.*
